# Camps workbook schema reference

Place updated Camp Research Master workbook exports here.

**This folder is schema/reference documentation only.**

- Not approved production seed data
- Not imported by public pages
- Used to reconcile provisional TypeScript types in `data/camps/types.ts`

When providing a workbook to Cursor, attach the file and state that it is for schema reconciliation only.
