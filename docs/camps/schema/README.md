# Camps workbook schema reference

Place updated Camp Research Master workbook exports here.

**This folder is schema/reference documentation only.**

- **Not** approved production seed data
- **Not** imported by public pages, CampCard previews, or fixtures
- Used only to reconcile provisional TypeScript types in `data/camps/types.ts`
- Row-level workbook content must not be copied into the public catalog without an explicit human approval pass

When providing a workbook to Cursor, attach the file and state that it is for **schema reconciliation only** — not seed data.

Accepted: `.md`, `.csv`, `.json` field dictionaries / column maps. Excel exports may be noted here with a companion markdown summary of columns.

See `provisional-domain-notes.md` for the current Prompt 1–3 modelling.
